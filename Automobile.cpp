//---------------------------------------------------------------------------

#pragma hdrstop

#include "Automobile.h"
//#include "SourceBook_Unit.h"
//---------------------------------------------------------------------------
Automobile::Automobile(const char *Table){

/*	SourceBook::TABLE_NAME = Table ;
	String str = String("SELECT KIND, CAPTION, RATING FROM ") ;
	str = str + Table + " " ;
	str = str + String("ORDER BY RATING DESC") ;

	SourceBook::BASE_REQUEST = str ;
	SourceBook::id = 0 ;
	SourceBook::rating = 0 ;
	SourceBook::name = "" ;

	// Конструктор класса Student
		student(
			std::string last_name,
			std::string name,
			std::string second_name,
			std::vector<int> scores
		) : human(
			last_name,
			name,
			second_name
		) {
			this->scores = scores;
		}
	*/
}
//---------------------------------------------------------------------------
Automobile::Automobile(){
}
//---------------------------------------------------------------------------
#pragma package(smart_init)
